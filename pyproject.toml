[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "xopr"
dynamic = ["version"]
description = "Access Open Polar Radar data in the Python data ecosystem"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "scipy",
    "xarray>=2024.10.0",
    "h5netcdf",
    "requests",
    "matplotlib",
    "aiohttp",
    "geoviews",
    "shapely",
    "geopandas",
    "h5py>=3.9",
    "fsspec",
    "dask[distributed]",
    "rustac>=0.6.0",
    "antimeridian>=0.4.3",
    "hdf5storage>=0.2.2",  # Requires Sept 2025 release with pkg_resources fix
    "duckdb>=1.0.0",
    "haversine>=2.8.0",
    "pyarrow",
    "platformdirs",
    "pyOpenSSL<24",  # gsutil incompatible with pyOpenSSL 24+ (removed crypto.sign)
]

[project.optional-dependencies]
stac = [
    "pystac",
    "stac_asset",
    "stac-geoparquet",
    "stac-fastapi-geoparquet[serve]",
    "omegaconf",
    "pyarrow",
    # mortie 0.8.1+ changed the morton index encoding (Rust/healpix migration),
    # breaking prefix matching in crossovers.py and bedmap/morton_match.py
    "mortie<0.8.1",
]
docs = [
    "mystmd>=1.10",
    "dask[distributed]",
    "coiled>=1.117.0",
    "ipykernel",
    "jupyter",
    "jupyterlab",
    "hvplot",
    "jupyter_bokeh",
    "rioxarray",
    "verde",
    "earthaccess",
    "pdoc>=14.0.0",
]
test = [
    "pytest",
    "pytest-cov",
    "flake8",
    "stac-validator",
    "xopr[stac]"
]
bench = [
    "pytest",
    "pytest-codspeed>=3.0",
    "xopr[stac]",
]

[tool.hatch.version]
source = "vcs"

[tool.hatch.build.hooks.vcs]
version-file = "src/xopr/_version.py"

[tool.pytest.ini_options]
addopts = "--cov=xopr --cov-report=xml --cov-report=term"
markers = [
    "slow: marks tests that require network access or large data (deselect with '-m \"not slow\"')",
    "integration: tests that download real data (may be slow)",
]
